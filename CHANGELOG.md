# 更新日志

## v0.4.0 (2026-05-18)

### 🔧 修复

- **打包修复：`services/` 目录遗漏** — `electron-builder` 的 `files` 配置未包含 `services/**/*`，导致打包后找不到 `git-service` 等模块
- **打包修复：asar 只读写入失败** — `config-service`、`log-service`、`main.js` 中所有 `__dirname` 路径改为 `app.getPath('userData')`，解决 `ENOTDIR` 报错
- **图片引用检测假阳性** — 改用完整路径匹配代替文件名匹配，解决同名不同目录图片误判为被引用的问题

### ✨ 改进

- **发布按钮自适应** — 单按钮根据 Git 状态自动切换：有待推送时显示"📤 推送"，有文件变更时显示"📝 提交"

---

## v0.2.0 (2026-05-13)

### ✨ 新功能

- **富文本编辑器工具栏大升级**：
  - 新增 **下划线** 按钮
  - 新增 **标题下拉选择**（H1 / H2 / H3 / 正文），替换原来的无效 H 按钮
  - 新增 **引用块** 按钮
  - 新增 **左/中/右对齐** 按钮
  - 新增 **插入链接** 按钮（弹窗输入 URL）
  - 新增 **分割线** 按钮
  - 新增 **清除格式** 按钮
- 正式加入 **electron-builder 打包配置**（NSIS 安装包 x64）

### 🔧 修复

- 修复"标题"按钮调用 `execCommand('heading')` 无效的问题，改用 `formatBlock` + 下拉选择器

### 🏗️ 基础设施

- 新增 `npm run pack` 打包脚本
- 新增 `package.json` 的 `build` 配置段
- 新增 CHANGELOG.md 版本记录
- 更新 README.md 项目说明

---

## v0.1.0 (初始版本)

- 多站点管理
- 文章 CRUD
- 产品管理
- 图片管理
- 分类管理
- 一键发布到 GitHub Pages
